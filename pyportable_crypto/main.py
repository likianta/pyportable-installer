def decompile(filename: str, encrypted_code: str):
    key = '...'
    
    def _decode(code, key) -> str:
        return ''
    
    plain_code = _decode(encrypted_code, key)
    
    # load into memory
    ...
