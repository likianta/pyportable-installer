# pip download -r requirements.txt -d examples/dist_pyportable_itself/requirements_offline
# pip download <package> -d examples/dist_pyportable_itself/requirements_offline

embed-python-manager
lk-logger >= 5.0.0
lk-utils >= 2.2.0
pyportable-crypto >= 1.0

# extra: see ./requirements-extra.txt
